## iOS

This directory is reserved for the future iOS app and keyboard-extension
implementation.

The intended direction is:

- native Swift app shell
- native keyboard extension
- shared core reuse from the repository's platform-neutral logic
