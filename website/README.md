## Website

This directory contains the static product site for Shinsoku.

Open `index.html` directly in a browser for local review, or deploy the folder
to any static host. The site is intentionally self-contained and keeps the
brand assets local to the directory.

Files:

- `index.html`
- `styles.css`
- `assets/square-bolt.svg`
