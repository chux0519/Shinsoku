## Website

This directory contains a static marketing site for Shinsoku.

Open `index.html` directly in a browser for local review, or deploy the folder
to any static host.

The site intentionally keeps its own files self-contained:

- `index.html`
- `styles.css`
- `assets/square-bolt.svg`
